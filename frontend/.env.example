# URL da API (produção: URL do backend no Vercel)
# Em desenvolvimento, deixe vazio para usar o proxy local (/api)
VITE_API_URL=

# Proxy do Vite em desenvolvimento (backend local)
VITE_API_PROXY=http://localhost:5000
